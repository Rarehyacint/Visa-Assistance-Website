# Visa-Assistance-Website

## Google sign-in

Copy `.env.example` to `.env` and set `VITE_GOOGLE_CLIENT_ID` to a Google OAuth 2.0 Web client ID. In Google Cloud Console, add the app origin (for example, `http://localhost:5173`) to the client's authorized JavaScript origins, then restart Vite.